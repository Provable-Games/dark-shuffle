pub mod draft {
    pub mod contracts;
    #[cfg(test)]
    pub mod tests;
}
pub mod game {
    pub mod contracts;
    #[cfg(test)]
    pub mod tests;
}
pub mod battle {
    pub mod contracts;
    #[cfg(test)]
    pub mod tests;
}
pub mod map {
    pub mod contracts;
    #[cfg(test)]
    pub mod tests;
}
pub mod season {
    pub mod contracts;
}
pub mod config {
    pub mod contracts;
    #[cfg(test)]
    pub mod tests;
}
